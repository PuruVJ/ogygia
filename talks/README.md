# Talks

## ogygia

Slidev deck (inventory + Remote Functions chapter):

```bash
pnpm -C talks/ogygia install
pnpm -C talks/ogygia dev
```

Presenter notes are on every slide — open presenter mode (`p` in Slidev) to read them.
