// Imported by a bundled <script island> to prove the script is a real module chunk
// (relative/npm imports resolve + bundle).
export function mark() {
	window.__bundledHelperMarked = (window.__bundledHelperMarked || 0) + 1;
}
