<script>
	// csr=false CLIENT host stand-in for a portable island binding.
	// SSR / csr=true keep the real wrapper; hydrate loads via import(entry) only.
	// Beside this stub the transform emits a pre-0.4-style host `__css` import of the
	// authored entry (`import __OgygiaFouc_X from '…'; void __OgygiaFouc_X`) so Kit still
	// collects island CSS into the page stylesheet graph.
</script>
