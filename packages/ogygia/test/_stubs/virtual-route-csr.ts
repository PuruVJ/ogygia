// Test stub for `virtual:ogygia/route-csr` — an empty set (no csr=true routes in unit tests). A suite
// that renders a csr=true document adds its route id here (and deletes it after).
export const csr_true_routes = new Set<string>();
