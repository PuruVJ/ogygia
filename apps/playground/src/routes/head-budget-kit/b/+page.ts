// The second csr=true page of the head-budget pair: reached by a Kit CLIENT-SIDE navigation from
// /head-budget-kit, it renders a registry block the first page never did (b) — the wrapper is
// created on the client, its component loads on demand and renders once it lands.
export const csr = true;
