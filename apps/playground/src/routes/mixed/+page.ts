// The combined bench: a slow I/O load AND a CPU-heavy render, so one report shows both the
// "Waiting by function" story and the "CPU / Components" story at once. Never prerendered.
export const prerender = false;
