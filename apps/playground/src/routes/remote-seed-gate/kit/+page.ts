// The csr=true twin: Kit hydrates this document and inlines the query itself (`__sveltekit_*.data`);
// ogygia emits no remote seed here. Same lake, same silent island, must work identically.
export const csr = true;
