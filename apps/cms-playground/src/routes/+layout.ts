// ogygia is csr=false (server-rendered islands). The corpus is REMOTE, so no prerender — every
// page renders on demand against the live CMS.
export const csr = false;
export const prerender = false;
