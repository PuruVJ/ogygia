// A Kit-hydrated page in the same app — makes the root layout's world MIXED, so the
// chrome islands must degrade inline here (Kit owns hydration) yet island on /content/*.
export const csr = true;
